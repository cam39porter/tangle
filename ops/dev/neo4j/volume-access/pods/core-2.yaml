}kind: Pod
apiVersion: v1
metadata:
  name: neo4j-volume-access-2
  labels:
    app: neo4j
spec:
  volumes:
    - name: datadir-neo4j-core-2	
      persistentVolumeClaim:
        claimName: datadir-neo4j-core-2	
  containers:
    - name: neo4j-volume-access-2
      image: us.gcr.io/opit-193719/dev-neo4j-access-image
      imagePullPolicy: "Always"
      volumeMounts:
      - name: datadir-neo4j-core-2	
        mountPath: /data
      command: ["/bin/bash", "-ecx", "while :; do printf '.'; sleep 5 ; done"]

